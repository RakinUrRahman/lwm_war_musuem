(function(){ function init(){ const grid=document.getElementById('favoritesGrid'); if(!grid||!window._LWM)return; const favIds=window._LWM.state.favorites||[]; const items=window._LWM.state.artifacts.filter(a=>favIds.includes(a.id)); grid.innerHTML=items.map(artifactCard).join('')||'<p class="muted">No favorites yet.</p>'; } if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,200)); else setTimeout(init,200); })();
